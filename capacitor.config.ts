import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IW2023tabseje02',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
